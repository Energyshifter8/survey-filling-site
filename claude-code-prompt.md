# Даалгавар: Dynamic судалгаа бөглөх веб апп бүтээх

## Контекст

Би динамик судалгаа (survey) бөглөх веб апп хөгжүүлж байна — судалгаанууд backend-ээс
ирдэг өгөгдлөөр (асуулт, сонголт, тоо) удирдагдах ёстой, кодонд hardcode хийгдэхгүй.

Одоо байгаа reference системийн network traffic-ыг судалж, дараах API урсгалыг
сэргээн гаргасан (200 OK-той бодит дуудлагууд, гэхдээ зарим талбарын нэр таамаглал):

1. `GET /s/{shortCode}` — судалгааны ID/metadata авах (auth хэрэггүй)
2. `POST /participate` — `{ survey: { id } }` явуулж, тухайн session/хэрэглэгч аль
   хэдийн бөглөсөн эсэхийг шалгах → `{ survey, taken: boolean }`
3. `POST /check-pass` — session эхлүүлж, `{ responseSessionId, token }` буцаана.
   `token` нь HS512 JWT бөгөөд payload дотор `surveyId` агуулсан. Эндээс хойшхи
   бүх дуудлага `Authorization: Bearer <token>` шаардана.
4. `GET /questions` — (Bearer шаардсан) асуултуудын жагсаалт татна
5. `POST /submit` — (Bearer шаардсан) хариултуудыг илгээх — **энэ endpoint-ийн
   яг нэр/бүтэц баталгаагүй, логик таамаглал**

Дагалдах side-call-ууд (заавал биш): `agent` (device/browser мэдээлэл), `beacon` /
`batch-events` (UX telemetry, `navigator.sendBeacon`-оор).

UX урсгал (reference screenshot-уудаас):
- **Intro** — гарчиг, тайлбар, судалгаа нийтлэгчийн нэр, "Эхлэх" товч
- **Consent** — асуултын тоо, ойролцоо хугацаа, зааварчилгаа, зөвшөөрлийн
  checkbox, checkbox чеклэгдсэний дараа идэвхжих "Цааш" товч
- **Questions** — дээд талд progress bar (%), асуулт дугаартайгаар, radio
  сонголтууд (single-select), сонголт хийсний дараа идэвхжих "Үргэлжлүүлэх" товч,
  сүүлийн асуулт дээр товч нь submit хийнэ
- **Done** — талархлын мессеж

## Одоо байгаа эх материал

Доорх 2 файлыг би аль хэдийн бэлдсэн — эдгээрийг эхлэл болгож ашигла, шаардлагатай
бол дахин зохион байгуул (rewrite чөлөөтэй, зөвхөн логик урсгал нь зөв байвал болно):

- `survey-client.js` — framework-agnostic fetch client (SurveyClient class),
  дээрх 5 алхмыг дараалуулан дуудна, token-ийг дотроо хадгалж Authorization
  header болгож дамжуулна.
- `SurveyFlow.jsx` — Next.js client component, дээрх 4 UI screen-ийг нэг
  компонент дотор state machine-аар удирддаг анхны хувилбар.

## Даалгаврын шаардлага

1. **Next.js (App Router) + React** дээр бүтээ. TypeScript ашиглаж болно
   (санал болгож байна) эсвэл plain JS — аль нь ч тохиромжтой бол сонгож болно,
   гэхдээ сонголтоо хэлж өг.
2. **Бодит route бүтэц** ашигла: `/s/[shortCode]` (intro + consent) ба
   `/s/[shortCode]/questions` (асуулт бүрчлэн) — эдгээрийн хооронд шилжихдээ
   session/token-оо алдахгүй байх (жишээ нь: server-side session, эсвэл
   client-side context/state + sessionStorage-ийн аль тохиромжтойг сонгож,
   яагаад тэрийг сонгосноо тайлбарла).
3. Судалгааны бүх контент (гарчиг, асуулт, сонголт, тоо, хугацаа) **backend-ээс
   dynamic ачаалагдана** — hardcode хийхгүй.
4. `participate` дуудлагад `taken: true` ирвэл шууд "already taken" төлөвийг
   харуулж, урсгалыг цааш явуулахгүй.
5. Алдааны төлөв бүрд (network error, expired token, 4xx/5xx) хэрэглэгчид
   ойлгомжтой, backend-ийн "дуу хоолой" биш интерфэйсийн энгийн үг хэллэгээр
   мессеж харуул (жишээ: "Холболт тасарлаа, дахин оролдоно уу" гэх мэт,
   уучлалт гуйхгүй, юу болсныг тодорхой хэлнэ).
6. Backend endpoint-уудын **яг нэр/бүтэц (ялангуяа `submit`) баталгаагүй** тул:
   - API дуудлагуудыг нэг `lib/api.ts` (эсвэл `.js`) файлд төвлөрүүлж, base URL-г
     `.env`-ээс уншуулж бай (`NEXT_PUBLIC_SURVEY_API_BASE` гэх мэт).
   - Хариу ирэх өгөгдлийн бүтцийг таамагласан хэсэгт `// TODO: confirm shape`
     гэсэн тайлбар бичиж, солиход хялбар болго.
   - Хэрэв надаас бодит API response жишээ авах шаардлагатай бол — **битгий
     таагаад цааш явж, надаас асуу.**
7. Загвар нь `SurveyFlow.jsx`-д эхлүүлсэн энгийн, цагаан дэвсгэртэй, нэг өнгийн
   (periwinkle-blue, ойролцоогоор `#7C83FD`) accent-той минимал стильтэй байна.
   Responsive байх ёстой (mobile-first), keyboard focus харагдахуйц байх ёстой.
8. `beacon`/telemetry дуудлагыг заавал бүрэн хийхгүй ч болно — гэхдээ дараа нь
   амархан нэмэгдэх боломжтой байдлаар код зохион байгуул (жишээ нь: нэг
   `trackEvent()` helper функц).

## Deliverable

- Ажиллаж байгаа Next.js төслийн бүтэц (`npm run dev`-ээр локал дээр ажиллах)
- `README.md`: суулгах/ажиллуулах заавар, `.env.example`, API contract-ын
  таамаглаж буй хэсгүүдийг тодорхой жагсаасан баримт
- Дараах кэйсүүдийг гараар туршиж шалгасан гэдгээ баталгаажуулах:
  intro → consent (checkbox идэвхгүй үед товч disable) → questions
  (progress bar шинэчлэгдэх, сонголт хийхгүйгээр цааш явахгүй) → done,
  мөн "already taken" болон нэг алдааны кэйс (жишээ нь: `check-pass` 401
  буцаасан үеийн харагдац)

## Чухал зарчим

Ойлгомжгүй/тодорхойгүй зүйл гарвал (жишээ нь backend response-ийн бодит
бүтэц, session хадгалах арга) таамаглаад дуугүй үргэлжлүүлэхийн оронд надаас
асуу. Хийсэн шийдвэр бүрийнхээ учрыг товч тайлбарла.
